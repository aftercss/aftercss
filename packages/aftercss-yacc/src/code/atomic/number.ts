import { BaseParser } from '../../base';
import { AtomicPlugin } from './plugin';

export class NegativeIntegerPlugin extends AtomicPlugin {
  public name: string = 'negativeInteger';
  public parse(parser: BaseParser) {
    // parser.matchReg()''
  }
}
