{
  "name": "@aftercss/test",
  "version": "0.1.8",
  "description": "aftercss",
  "main": "index.js",
  "scripts": {
    "test": "node -r esm ./node_modules/.bin/_mocha index.js",
    "test:clean": "cd fixtures/aftercss-parser && aftertest clean && cd ../aftercss-tokenizer && aftertest clean &&cd ../remove-comments && aftertest clean  "
  },
  "private": true,
  "dependencies": {
    "@aftercss/parser": "^0.1.8",
    "@aftercss/shared": "^0.1.8",
    "@aftercss/tokenizer": "^0.1.8",
    "after-test": "^1.0.8",
    "esm": "^3.2.25"
  },
  "devDependencies": {
    "after-test": "^1.0.8",
    "mocha": "^5.2.0"
  }
}
